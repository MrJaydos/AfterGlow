export const WORLD_W = 4096;
export const WORLD_H = 720;
export const WORLD_GRAVITY = 900; // px/s² — must match physics config in main.ts
export const TICK_RATE = 60;
export const FIXED_DT_MS = 1000 / TICK_RATE; // ~16.667 ms per fixed tick
