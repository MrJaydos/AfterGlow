# Server
PORT=3000
NODE_ENV=development

# Database (persistent volume path in production)
DB_PATH=/data/afterglow.sqlite

# Admin API (change in production)
ADMIN_TOKEN=change-me-in-production

# CORS — comma-separated allowed origins, or * for all
CORS_ORIGIN=*
